# Trip Mapper v1.0 — Environment Variables
# Copy this file to .env and adjust as needed.

# Set to "false" to prevent normal log writes when this project is built.
# The explicit setLoggingEnabled(true|false) status message can still print.
# Default: enabled (logging is on unless explicitly disabled).
# VITE_LOGGING_ENABLED=false
