# Trip Mapper v1.0 — Environment Variables
# Copy this file to .env and adjust as needed.

# Set to "false" to disable logging at build time (dead-code elimination)
# When disabled, zero console output is produced and no log network requests are made.
# Default: enabled (logging is on unless explicitly disabled)
# VITE_LOGGING_ENABLED=false
